{
  "name": "graphql-cn-group-talk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "BROWSER=none react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && surge -p ./build --domain cn-graphql-in-2018.surge.sh"
  },
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "spectacle-scripts": "2.0.0"
  },
  "devDependencies": {
    "prettier": "^1.13.5"
  }
}
